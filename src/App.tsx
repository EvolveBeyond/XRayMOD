/**
 * Legacy Vite SPA stub — canonical UI is `frontend/` (Next.js).
 * Telegram Mini App / TON / store surfaces were removed from the product.
 */
export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>
      <h1>XRayMOD</h1>
      <p>This Vite shell is legacy. Use the Next.js panel under <code>frontend/</code>.</p>
    </div>
  );
}
