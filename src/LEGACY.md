# Legacy Vite SPA

This directory (`src/`, root Vite config) is **legacy**.

**Canonical panel UI:** `frontend/` (Next.js 15 App Router, static export).

Telegram Mini App, store/commerce, and TON wallet UI have been removed from the product.
Do not reintroduce them here — build features in `frontend/` + `worker/` only.
